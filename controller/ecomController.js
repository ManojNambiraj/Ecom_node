const Model = require("../model/ecomModel")

const controller = {
    async productList(req, res){
        try {
            
        } catch (error) {
            console.log(error);
        }
    }
}

module.exports = controller;